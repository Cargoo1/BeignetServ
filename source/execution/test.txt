1-
	valid.conf ->	listen					127.0.0.1; (l:2);
	gai: Servname not supported for ai_socktype
	Forcement un port (8080) ? Il n'y avait pas une histoire ou on pouvait mettre soit port soit IP ?

2-
	"POST / HTTP/1.1\r\nHost:localhost:8080\r\nContent-Length:10\r\n\r\n1234567890";
	pas de throw bad_request.
	Devrait être géré dans "int	handle_request(Client& client, std::vector<serverConfig> const& serverConf)"
	Non ?

3-
	"GET /okaodjaiai HTTP/1.1\r\nHost:localhost:8080\r\nContent-Length:10\r\n\r\n1234567890"
	Redirect old_page 301 ou 404 ?
	Actuellement 301.

4-
	Dans handle_request, ne serais-ce pas mieux de creer la reponse ici puis de verifier parser la requet.
	Comme ca la gestion du code erreur pourrait-être plus simple a gérer ?

Le rest:
	POST et DELETE semble fonctionner. Attention cependant a DELETE qui supprime réellement les fichiers.
	Il peut avoir des petit missfunction dans la gestion du path. il faudrait faire attention a ca.
	Il est possible que qu'on se soit mal compris sur la gestion des requet. Il faudrait faire un point sur
	qui fait quoi, et remettre le code a jour. Autrement la majorité de l'éxecution semble plus ou moins
	fonctionelle.

	Dans "locationConfig const&	find_location_block(std::string const& uri, std::vector<locationConfig> const& loc_confs)" il faudrait ajouter
	la gestion du 404. Si le bestmatch ne depasse pas le root du bloc serv.
	J'ai essayé de faire par vector dans -> <HttpMethodDispatcher.hpp>
	Dis moi ce que tu en pense.

	Peut-être plus simple de faire un rooter tel que
	request-> path ? oui : Non
		oui continue
		non 404
			method allowed ? oui : non
				oui continue
				non 400
					client_max_body_size ? oui : non
						oui exec
						non 415
					
